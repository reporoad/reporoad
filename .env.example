# Production values belong in Sites runtime settings, never in source control.
# GitHub user authorization: fixed client ID and callback are in lib/github-auth.ts.
GITHUB_CLIENT_SECRET=
# Generate a high-entropy value, at least 32 characters; rotation invalidates sessions.
GITHUB_SESSION_SECRET=
